type Props = {
    name: string
}

const SchoolAdd = ({name} : Props) => {
    return (
        <div className="">
            <span>{name}</span>
            <div className="">
                <i />
                <i />
                <i />
            </div>
        </div>
    )
}

export default SchoolAdd;